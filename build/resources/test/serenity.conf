serenity {
  take.screenshots = FOR_FAILURES
  headless.mode = true

  webdriver {
    driver = "chrome"
    autodownload = true
    remote.enabled = false

    capabilities {
      "goog:chromeOptions" {
        args = ["--remote-allow-origins=*"]
      }
    }
  }
}

